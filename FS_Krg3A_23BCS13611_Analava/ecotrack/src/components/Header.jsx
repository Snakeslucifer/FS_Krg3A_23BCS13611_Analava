const Header = ({title})=>{
    return<>
    <header style={{padding: "0.5rem",backgroundColor: "#3a803cff", color: "white", textAlign: "center"}}>
    <h1>{title}</h1>
    </header>
    </> 
}

export default Header;